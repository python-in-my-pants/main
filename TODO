1 - !!!!!!!!!!!disclaimer!!!!!!!!!!!!!

1 - https://stackoverflow.com/questions/3344115/how-to-obfuscate-python-code-effectively

1 - (BUG) spritzen aus char select raus

1 - (CONTENT) LOSE BANNER ÄNDERN

1 - (CONTENT) OBEN RECHT PLAYER BANNER + HOST/CLIENT

1 - (VISUAL) finish item and weapon card artworks

1 - (CONTENT) fucking fix accuracy OwO

1 - (PERFORMANCE) overlay does not open on first click on opponent char sometimes

    1 - (BUG) moven nach dem schießen oder einfach nur moven crasht mit infinite?

    1 - (BUG) overlay ist manchmal leer (nach kill?)

    1 - (BUG) tode sind nicht synchron?

1 - man kann noch durch büsche kucken

1 - (CONTENT) büsche op af ???

1 - (CONTENT) delete stamina from card images

1 - (CONTENT) shot gear hp next to gear card (ganz unten links im bild) as vertical hp bar

1 - (CONTENT) if bleeding blit small red drop in upper left corner of char detail card surface

1 - (CONTENT) change item/gear/weapon card in main game screen to those with stats

IDEAS:

weapon weight influences hipfire accuracy
more mass = more hp?
area damage for rpg
bloom?
loot dead chars: if own sel char and click on dead char (opp/own) and click on inventory and this slot in own sel char
    is not full: add item/gear/weapon to own inventory

-----------------------------------------------------------------

2 - (PERFORMANCE) deactivate buttons while waiting for opp turn

2 - (BUG) hosted game isn't shown when hosting a game after finishing a game

2 - (VISUAL) blit little white cross with red outline above upper right of opp char that has healed in apply turn

2 - (BUG) mehrmals ready drücken ist schlecht - 2. ready muss alles löschen, was das erste gesetzt hat

2 - (CONTENT) nur sichtbarkeitslinien bis zu letztem sichtbaren punkt

2 - (CONTENT) amphetamine as heal item, which you can use to shoot even if you have no arm hp left or move even if you got no leg
    hp left but damages your torso hp over time (like bleed) damage

2 - (VISUAL) eye candy canvas for ingame screen, 1 single pic with alpha as background???

2 - (VISUAL) add sick loading screens with 99% loading bars that never actually fill, like in connection setup when opp is joining

2 - (VISUAL) outline selected char in team selection (red canvas)

2 - (PERFORMANCE) doublebuf, hwsurface in set_mode for better performance

2 - (PERFORMANCE) make turn time dependent on max chars that are actually in one of the teams

2 - (CONTENT) calc bleed damage every turn

2 - (CONTENT) add custom fields to enter map size and lobby name

2 - (BUG) server: unhashable type: connection (wenn 1 client crasht) -> bist du noch da? msg

(2 - differenziere bei inventory buttons zwischen own char (kann switchen mit buttons) und gegner (der kann das nicht lol))

2 - (BUG) inventory does not blit correctly after having selected an opponent char
    (prolly just go for sel_own_char, sel_opp_char)

2 - (CONTENT) integrate occupied-by-opp-fields into r_fields but dont move to them, just to adjacent field OR
    move until you can see them and let user decide again what he wants to do

2 - (VISUAL) attack overlay does not shift yet (when zoomed)

todo reset 684

todo take out server debug print

clear all TODO

--------------------------------------------------------------------------

3 - (CONTENT) usage system for items, set remaining uses to 0 when depleted and weight to 0

3 - (CONTENT) build complex houses
    generate random graph, nodes are rooms, edges are doors, 1 outdoor node connected to min 1 random node
    put in rooms peu a peu in an intermediate surface

3 - (CONTENT) use Bebauungsplan for setting objects on map

3 - (CONTENT) make selected char look at mouse

3 - (PERFORMANCE) blit only parts that may have changed in main screen (right & left ui are likely to not change most of the frames)

3 - (PERFORMANCE) use buffers for blitting in main screen? (flags

3 - (CONTENT) Add animated start screen
    https://stackoverflow.com/questions/21356439/how-to-load-and-play-a-video-in-pygame

3 - (PERFORMANCE) join ausgrauen wenn gehostet wird

3 - (PERFORMANCE) cancel join automatisch wenn kein game zum joinen ausgewählt war

3 - (CONTENT) (directional) velocity that influences how far you can run in this turn

3 - (CONTENT) interior

3 - (PERFORMANCE) find better solution for unique ids than randint

3 - (PERFORMANCE) for everything related to blitting on the map: just blit it on self.game_map.surface and resize afterwards

3 - (CONTENT) directional looking (shooting/visibility)

3 - (PERFORMANCE) time delay between sending turns over (old turns get spammed out and spam the network, preventing this should help)